apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: romang-test 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 3001
